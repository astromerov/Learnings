/*

Pretty much every time you see a () parentheses without for loop or any other conditionals
you are making a use of a function.

*/

const myText = "I am a string";
const newString = myText.replace("string", "sausage");

console.log(newString);

// The output is "I am a sausage" because its taking the string word and replacing it using the replace() function

/*

Or everytime when an array is manipulated 

*/

const myArray = ['I', 'love', 'array'];
const madeaString = myArray.join("");

console.log(madeaString);

// this code joins the strings together and make it in one line.


// Generate a random number

const number = Math.random();       // this code generates a random number from 0 and up but not the number 1










