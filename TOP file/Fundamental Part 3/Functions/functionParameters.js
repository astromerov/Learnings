/*

Function parameters are also sometimes called arguements, properties, or even attributes.

*/

// Math.ranodm() - is a built-in javascript function and doesnt require a parameter.

const myNumber = Math.random();

// replace() - this function requires two parameters, the substring to find the main string, and the substring to replace with.

const string = ["stringOne", "stringTwo", "stringThree"]
const destring = string.replace("stringOne", "stringUan")

/* the code above is taking the array of "stringOne", and replacing it with another string "strinUan" using the replace() function */



























