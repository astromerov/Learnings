/*

Null is something like "empty", "unknown", or "nothing".
But, its not like other programming languages that describe "referenec to a non-existing project"

*/

let age = null;     // Outcome: null, because it is undefined or something similar

/*

The undefined value:
When "undefined" is the outcome, the result is like "value is not assigned"

*/

let age1;
alert(age1);         // Outcome: undefined; because, the value above is just declaring the object, but nothing is given to it











