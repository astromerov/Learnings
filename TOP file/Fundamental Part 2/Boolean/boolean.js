/*

The boolean type.
It has only two values; true, which means "yes, correct", and false which means "no, incorrect"

*/

// For instanec

let myNameIs = true;        // Outcome: true
let mySurnameIs = false;    // Outcome: false

// Boolean values also come as a result of comparisons

let isGreaterValue = 4 > 1;
alert(isGreaterValue);          // true; because, 4 is greater than 1